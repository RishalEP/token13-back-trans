apiVersion: v1
kind: Service
metadata:
  name: back-trans-dev
  namespace: dev
spec:
  selector:
    app: back-trans-dev
  ports:
    - name: http-4444
      port: 4444
      targetPort: 4444
  type: ClusterIP