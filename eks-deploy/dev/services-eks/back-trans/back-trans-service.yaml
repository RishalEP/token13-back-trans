apiVersion: v1
kind: Service
metadata:
  name: back-trans-dev
  namespace: dev
spec:
  selector:
    app: back-trans-dev
  ports:
    - name: http-8800
      port: 8800
      targetPort: 8800
  type: ClusterIP